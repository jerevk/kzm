{
  "name": "kzm-novi",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.16.0"
  },
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "deploy": "node scripts/deploy.mjs",
    "import": "node scripts/import.mjs",
    "backup": "node scripts/backup.mjs",
    "football-key": "node scripts/football-key.mjs",
    "test": "node --test tests/*.test.mjs",
    "demo": "node scripts/demo.mjs",
    "dev": "wrangler dev",
    "check": "node --check src/worker.js && node --check src/db.js && node --check src/services/onesignal.js && node --check src/football.js && node --check public/app.js && node --check public/js/push.js && node --check scripts/deploy.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.34.0"
  }
}
